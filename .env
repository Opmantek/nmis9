MONGODB_USERNAME=
MONGODB_PASSWORD=
NMIS_SERVER_NAME=nmis
NMIS_CLUSTER_ID=
# NMIS_DB_PORT removed here (OMK-12708). It was only ever read by compose.yaml,
# and the app's db_port now comes from MONGODB_PORT below, which also drives
# mongod's own port and the container side of the published mapping, so the two
# cannot disagree. Only this file ever defined it, so the other two stacks were
# silently relying on the Config.nmis default.

# OMK-12708. Where the Mongo container's port is published on the host.
#
# Read this before changing MONGODB_BIND_ADDR. Docker publishes ports by
# writing its own NAT rules, which are evaluated before the host firewall, so
# a published port is reachable even when iptables or ufw is set to deny it.
# The previous default published on every interface, which put the database
# holding your device data and credentials directly on the network.
#
# 127.0.0.1 keeps it reachable from this host only, which is all that backups,
# mongosh and the host-side test runs need. Set it to a specific address if a
# named remote host must reach the database, and firewall that address at the
# network rather than trusting the host firewall. 0.0.0.0 restores the old
# exposed behaviour and should not be used.
#
# One caveat, on Docker Engine older than 28.0.0 only. Docker's documentation
# warns that "In releases older than 28.0.0, hosts within the same L2 segment
# (for example, hosts connected to the same network switch) can reach ports
# published to localhost" (moby/moby#45610). So on an older engine 127.0.0.1 is
# not a complete boundary. Upgrade the engine, or firewall 27017 at the network.
MONGODB_BIND_ADDR=127.0.0.1

# Host-side port. Change this to run more than one stack on a host, or to move
# Mongo off the well-known port. The container-side port stays 27017.
MONGODB_HOST_PORT=27017

# OMK-12708. How the NMIS container reaches Mongo, fed to the app as
# NMIS_DB_SERVER / NMIS_DB_PORT. NMIS overrides any config key from NMIS_<KEY>
# in the environment, so these take effect without editing conf/Config.nmis.
#
# These are the CONTAINER side, i.e. inside the compose network. They are NOT
# the same as MONGODB_BIND_ADDR / MONGODB_HOST_PORT above, which are only about
# where the port is published on the host. Pointing the app at the host port
# would make it dial a port mongod is not listening on. MONGODB_PORT drives the
# container port everywhere at once: mongod's --port, the container side of the
# published mapping, the healthcheck, and NMIS_DB_PORT.
#
# MONGODB_SERVER only redirects the APP. It does not reconfigure the bundled
# mongo service, whose own identity stays 'mongo' (its hostname on the compose
# network, its bindIp, and its healthcheck). Pointing this at an external
# database means the bundled mongo service should not be running at all.
MONGODB_SERVER=mongo
MONGODB_PORT=27017

# ---------------------------------------------------------------------------
# OMK-12708. Running more than one NMIS stack on one host.
#
# Everything the host can see is a variable, defaulting to today's value, so a
# second stack needs only a different env file and no compose edits:
#
#   COMPOSE_PROJECT_NAME  isolates volumes, the network and default container
#                         names. Compose reads it natively from this file. Set
#                         it first; without it two stacks started from the same
#                         directory share volumes and will corrupt each other.
#   NMIS_CONTAINER_NAME   host-level name of the app container
#   MONGO_CONTAINER_NAME  host-level name of the database container
#   NMIS_BIND_ADDR        host address the app's ports are published on
#   NMIS_HTTP_PORT        web UI published port (container side stays 8080)
#   NMIS_SNMP_PORT      trap/SNMP listener published port (container side 161/udp)
#   MONGODB_BIND_ADDR / MONGODB_HOST_PORT   where Mongo is published
#   MONGODB_SERVER / MONGODB_PORT           how the app reaches Mongo
#   MONGODB_USERNAME / MONGODB_PASSWORD     database credentials
#   NMIS_IMAGE / MONGO_IMAGE                image tags, to run two versions
#
# The container_name variables are host-level labels only. Internal DNS uses the
# SERVICE name, so renaming them does not affect how the app reaches Mongo or
# what mongod's bindIp resolves. Verified.
#
# A worked second stack, alongside a default one:
#
#   COMPOSE_PROJECT_NAME=nmis-b
#   NMIS_CONTAINER_NAME=nmis-b
#   MONGO_CONTAINER_NAME=mongo-b
#   NMIS_HTTP_PORT=8081
#   MONGODB_HOST_PORT=27018
#   NMIS_SNMP_PORT=10002
# MONGODB_SERVER and MONGODB_PORT do NOT need changing: they are inside the
# stack's own network, where each project has its own Mongo on the standard port.
# ---------------------------------------------------------------------------
NMIS_CONTAINER_NAME=nmis
MONGO_CONTAINER_NAME=mongo
NMIS_BIND_ADDR=0.0.0.0
NMIS_HTTP_PORT=8080
NMIS_SNMP_PORT=10001
